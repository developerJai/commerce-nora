<%# 4-Column Equal Grid - Perfect for category showcases and promotional cards %>
<div class="grid grid-cols-2 md:grid-cols-4 gap-3 sm:gap-4">
  <% items.first(8).each do |item| %>
    <%= render "home/layouts/collection_item", item: item, aspect: "h-52 sm:h-64 md:h-72" %>
  <% end %>
</div>
