<%# 2-Column Grid - Bold, statement-making layout %>
<div class="grid grid-cols-1 sm:grid-cols-2 gap-3 sm:gap-5">
  <% items.first(4).each do |item| %>
    <%= render "home/layouts/collection_item", item: item, aspect: "h-52 sm:h-64 md:h-80" %>
  <% end %>
</div>
