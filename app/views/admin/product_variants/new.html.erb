<% content_for(:title) { "New Variant" } %>

<div class="max-w-6xl mx-auto">
  <div class="mb-6">
    <h1 class="text-2xl font-bold text-gray-900">New Variant</h1>
    <p class="text-sm text-gray-500 mt-1"><%= @product.name %></p>
  </div>

  <%= render 'form' %>
</div>
