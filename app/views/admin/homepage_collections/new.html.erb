<% content_for(:title) { "New Collection" } %>

<div class="max-w-2xl">
  <h1 class="text-2xl font-bold text-gray-900 mb-6">New Homepage Collection</h1>
  <%= render 'form', collection: @collection %>
</div>
