<% content_for(:title) { "New Bundle Deal" } %>

<div class="space-y-6">
  <div class="flex items-center justify-between">
    <h1 class="text-2xl font-bold text-gray-900">New Bundle Deal</h1>
    <%= link_to 'Cancel', admin_bundle_deals_path, class: 'px-4 py-2 border border-gray-300 text-gray-700 rounded-md hover:bg-gray-50' %>
  </div>

  <%= render 'form', bundle_deal: @bundle_deal %>
</div>
